import { ExpDateDirective } from './exp-date.directive';

describe('ExpDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpDateDirective();
    expect(directive).toBeTruthy();
  });
});
